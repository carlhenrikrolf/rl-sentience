[build-system]
requires = ["uv_build >= 0.11.7, <0.12.0"]
build-backend = "uv_build"

[project]
name = "rl-sentience"
version = "0.0.0"
dependencies = [
    "ipykernel >= 7.2.0",
    "brainrender >= 2.1.20"
]